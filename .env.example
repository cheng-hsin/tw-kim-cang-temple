# 後台登入需要的環境變數。
# 複製這個檔案成 .env.local(不要 commit .env.local),把下面的值換成自己的:

# 後台登入的帳號密碼(單一組,整個寺院共用)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=請換成一組夠複雜的密碼

# 用來簽署登入 session 的密鑰,隨便一串夠長、夠亂的字串即可
# 可以用這個指令產生: openssl rand -base64 32
SESSION_SECRET=請換成隨機字串

# (選填)後台上傳的圖片/PDF/EPUB 要不要順便 commit 備份到 GitHub。
# 沒設定 GITHUB_TOKEN 的話這個功能就是關閉的,上傳只會寫到本機/伺服器的 public 資料夾。
# GITHUB_TOKEN:去 https://github.com/settings/tokens 建立 fine-grained token,
#   Repository access 選這個 repo,Permissions 給 Contents: Read and write。
# GITHUB_REPO_OWNER / GITHUB_REPO_NAME / GITHUB_BRANCH:不填的話預設是
#   cheng-hsin / tw-kim-cang-temple / main。
GITHUB_TOKEN=
GITHUB_REPO_OWNER=
GITHUB_REPO_NAME=
GITHUB_BRANCH=
