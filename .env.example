# 後台登入需要的環境變數。
# 複製這個檔案成 .env.local(不要 commit .env.local),把下面的值換成自己的:

# 後台登入的帳號密碼(單一組,整個寺院共用)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=請換成一組夠複雜的密碼

# 用來簽署登入 session 的密鑰,隨便一串夠長、夠亂的字串即可
# 可以用這個指令產生: openssl rand -base64 32
SESSION_SECRET=請換成隨機字串
